# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'Parkerly' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Parkerly

  # I know that using third-party libraries is discouraged for this assignment,
  # but I still want to list some I would want to use.
  #
  # RxSwift
  # Alamofire + Moya

  target 'ParkerlyTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
